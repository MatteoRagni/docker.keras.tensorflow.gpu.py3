# Tensorflow / Keras / Python 3 GPU Enabled Docker Image

This repository contains a `Dockerfile` for

 * Tensorflow in version `latest-gpu-py3`
 * Keras (installed through `pip`)
 * CUDA GPU supports

It needs `nvidia-docker` to run properly.
